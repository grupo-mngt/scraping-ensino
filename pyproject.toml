[project]
name = "scraping-ensino"
version = "0.1.0"
description = "Scraper de documentos e vídeos de plataforma de ensino via Playwright."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "playwright>=1.59.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.0",
    "static-ffmpeg>=3.0",
    "yt-dlp>=2026.3.17",
]

[tool.uv]
package = false
